<?xml version="1.0" encoding="utf-8"?>
<!--
   Data extraction rules for Android 12+ (API 31+)
   See https://developer.android.com/about/versions/12/backup-restore
-->
<data-extraction-rules>
    <cloud-backup>
        <!-- Include all shared preferences (DataStore uses this) -->
        <include domain="sharedpref" path="."/>

        <!-- Exclude EncryptedSharedPreferences - cannot be restored on different device -->
        <exclude domain="sharedpref" path="ai_helper_secure_prefs.xml"/>

        <!-- Include DataStore preferences specifically -->
        <include domain="file" path="datastore/"/>
    </cloud-backup>

    <device-transfer>
        <!-- Include preferences for device-to-device transfer -->
        <include domain="sharedpref" path="."/>

        <!-- Exclude EncryptedSharedPreferences - cannot be restored on different device -->
        <exclude domain="sharedpref" path="ai_helper_secure_prefs.xml"/>

        <include domain="file" path="datastore/"/>
    </device-transfer>
</data-extraction-rules>